import { Button } from '@/components/ui/button';

export default function Home() {
  return (
    <div>
      <Button
        className="mx-auto mt-10 block cursor-pointer"
        variant={'outline'}
      >
        В корзину
      </Button>
    </div>
  );
}
